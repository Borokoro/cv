import 'package:flutter/material.dart';

class NameAndPhotoWidget extends StatelessWidget {
  const NameAndPhotoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
